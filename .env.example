# Aplicação
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tasksdb

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# Cache (opcional)
REDIS_TTL=30

# SSE / Config extra (opcional)
SSE_RETRY=3000